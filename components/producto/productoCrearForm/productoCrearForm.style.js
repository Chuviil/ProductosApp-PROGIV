import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    btnContainer: {
        width: '80%',
        alignItems: 'center',
        marginTop: 15
    },
});

export default styles;
