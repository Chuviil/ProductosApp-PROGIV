import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    formContainer: {
        width: '80%',
        position: 'relative',
    },
    inputContainer: {
        width: '100%',
        height: 40,
        borderRadius: 5,
        padding: 2,
        borderWidth: 1,
        borderColor: '#000',
        borderStyle: 'solid'
    },
});

export default styles;
